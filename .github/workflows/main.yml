name: CI

on:
  push:
    branches: main
  pull_request:
    branches: main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu:22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: echo
        run: echo "Hello Github Actions!"
  
